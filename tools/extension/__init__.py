# Chrome-extension publisher package. Drains Postgres staging tables
# (extension_staged_approvals, pending_new_retailers) into local CSVs and
# the new-retailer queue file. Always runs locally; never on Railway.
